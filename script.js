const navsection = document.querySelector(".nav-section");
const navlinks = document.querySelector(".nav-links");

navlinks.style.paddingRight =
  navlinks.offsetWidth - navlinks.clientWidth + "px";
